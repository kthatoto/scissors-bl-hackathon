<style>
  .message.closable {
    position: absolute;
    top: 60px;
    left: 0;
    right: 0;
    max-width: 450px;
    margin: auto;
  }
</style>
<% flash.each do |key, value| %>
  <div class="<%= flash_class(key) %> closable">
    <i class="close icon"></i>
    <%= value %>
  </div>
<% end %>
