<style>
  .message.closable {
    position: absolute;
    top: 50px;
    left: 20%;
    right: 20%;
  }
</style>
<% flash.each do |key, value| %>
  <div class="<%= flash_class(key) %> closable">
    <i class="close icon"></i>
    <%= value %>
  </div>
<% end %>
