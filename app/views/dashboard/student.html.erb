
<div class="ui page grid">
  <div class="row">
    <div class="column">
      <div class="ui huge message page grid center aligned">
        <h1 class="ui huge header">プロフィール登録</h1>
        <p>あなたの情報を登録してください。</p>
      </div>
    </div>
  </div>

  <div class="one column row">
    <%= render 'user_card', page: :student %>
  </div>

  <div class="row">
    <div class="column">
      <div class="ui huge message page grid center aligned">
        <h1 class="ui huge header">自動車・メンターを探す</h1>
        <p>検索をして借りることができる自動車や教えてくれるメンターを探しましょう。</p>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="example" style="margin:0 auto;">

      <%= form_with url: [:search, :opportunities], local: true, class: "ui form" do |f| %>
        <div class="field">
          <label>日にち</label>
          <%= f.select :date, [["今日", 0], ["3日以内", 3], ["1週間以内", 7], ["2週間以内", 14], ["1ヶ月以内", 30]], {}, {class: "ui dropdown"} %>
        </div>

        <div class="field">
          <label>地域</label>
          <%= f.text_field :region, placeholder: "東京都" %>
        </div>

        <div class="field">
          <label>自動車名称</label>
          <%= f.text_field :region, placeholder: "フリード" %>
        </div>

        <%= f.submit "検索", class: "ui button primary" %>
      <% end %>

    </div>
  </div>

  <%= render 'layouts/footer' %>
</div>
