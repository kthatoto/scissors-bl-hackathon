
<div class="ui page grid">
  <div class="row">
    <div class="column">
      <div class="ui huge message page grid center aligned">
        <h1 class="ui huge header">プロフィール登録</h1>
        <p>貸すことができる車、あなたの情報を登録してください。</p>
      </div>
    </div>
  </div>

  <div class="two column row">
    <div class="column">
      <div class="center aligned starter column">
        <center><h2 class="ui header">自動車情報登録</h2></center>
        <div class="ui card"style="margin: 0 auto;">
          <div class="image" style="padding: 20px;">
            <%= image_tag "car_s.png" %>
          </div>
          <div class="content">
            <a class="header"><%= @current_user.name ? @current_user.name : "名前未設定" %></a>
            <div class="meta">
              <span class="date"><%= @current_user.gender_jp %></span>
            </div>
            <div class="description"><%= @current_user.region_jp %></div>
          </div>
          <div class="extra content">
            <%= link_to :root do %>
              <i class="user icon"></i> 編集
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <%= render 'user_card', page: :tutor %>
  </div>

</div>
